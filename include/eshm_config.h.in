/* ESHM Configuration Header - Generated by CMake */
#ifndef ESHM_CONFIG_H
#define ESHM_CONFIG_H

/* Memory Layout Configuration */

/* Maximum data size per channel in bytes
 * Override with: cmake -DESHM_MAX_DATA_SIZE=8192 ..
 * Default: 4096 bytes
 */
#define ESHM_MAX_DATA_SIZE @ESHM_MAX_DATA_SIZE@

/* Heartbeat update interval in milliseconds
 * Override with: cmake -DESHM_HEARTBEAT_INTERVAL_MS=2 ..
 * Default: 1 ms
 */
#define ESHM_HEARTBEAT_INTERVAL_MS @ESHM_HEARTBEAT_INTERVAL_MS@

/* Derived memory layout information */

/* Total size of ESHMChannel structure (approximately):
 * seqlock (4) + data_size (4) + data (ESHM_MAX_DATA_SIZE) +
 * write_count (8) + read_count (8) + padding (48)
 * Aligned to 64 bytes
 */
#define ESHM_CHANNEL_SIZE_APPROX (72 + ESHM_MAX_DATA_SIZE)

/* Total shared memory size (approximately):
 * header (64) + master_to_slave channel + slave_to_master channel
 */
#define ESHM_TOTAL_SIZE_APPROX (64 + 2 * ESHM_CHANNEL_SIZE_APPROX)

#endif /* ESHM_CONFIG_H */
